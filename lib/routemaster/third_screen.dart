import 'package:flutter/material.dart';

class RouterMasterThirdScreen extends StatelessWidget {
  const RouterMasterThirdScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Third'),
    );
  }
}
